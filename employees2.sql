CREATE TABLE employees
( 
emp_no int NOT NULL,
emp_title varchar,
birth_date date not null,
first_name varchar,
last_name varchar,
sex varchar,
hire_date date,
PRIMARY KEY(emp_no));

SELECT * from employees;
